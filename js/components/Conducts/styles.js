import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    overflow: 'hidden',
  },
  conduct: {
    flexDirection: 'row',
    marginVertical: 10,
  },
  indicator: {
    marginRight: 10,
    fontSize: 16,
    color: '#9963ea',
    fontWeight: 'bold',
  },
  description: {
    lineHeight: 20,
    fontSize: 16,
  },
});

export default styles;
