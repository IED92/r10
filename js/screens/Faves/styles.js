import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: '100%',
    alignItems: 'center',
    marginTop: 100,
  },
  text: {
    fontWeight: 'bold',
  },
});

export default styles;
